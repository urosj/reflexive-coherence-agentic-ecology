# P2-I2 Shared-Pool Co-Conditioning Brief

**Status:** accepted P2-I2 theory, geometry, ecology, and observation directive;
accepted by the project owner on 2026-07-14; three-mode retention scope
correction accepted by the project owner on 2026-07-14; Appendix A accepted
and closed under DEC-074/CHG-079 on 2026-07-15 after one complete APP-A2
runtime/reconstruction cycle; prospective Appendix B compact-cycle authority
accepted under DEC-075/CHG-080 on 2026-07-15; APP-B2/B3 technically complete
with owner result acceptance pending; APP-B4 completes its 75-arm runtime,
retained-only reconstruction, and closeout under DEC-092–093/CHG-097–098 and
is owner-accepted/closed under DEC-094/CHG-099

**Lane:** `AE01-L02`

**Hypothesis:** `AE01-H-L02`

**Evidence effect:** none; this brief defines how to look, not what was found

**Controlling contracts:**
[lane hypothesis](../hypotheses/lane-hypotheses.md),
[post-R3 ecology-discriminator amendment](../hypotheses/post-r3-ecology-discriminator-amendment.md),
[outcome and stopping contract](../hypotheses/outcome-and-stopping-contract.md),
[developmental interpretation contract](../hypotheses/developmental-interpretation-contract.md),
[primary metric sheet](../contracts/metric-sheets/AE01-L02.json),
[P1-I5 tooling contract](P1-I5-tooling-contract.md), and
[Review R3](../reports/R3-contract-adequacy-review.md)

**Acceptance and process records:**
[P2-I2 decision record](P2-I2-decision-record.md),
[P2-I2 checklist](P2-I2-shared-pool-co-conditioning-checklist.md), and
[operational hypothesis projections](../hypotheses/p2-i2-operational-hypotheses.md)

## 1. Role and authority

This brief supplies the semantic and observational center for P2-I2. It asks
what shared-pool co-conditioning could mean geometrically and ecologically,
what relation must distinguish it from repeated P2-I1-style writing, which
questions must remain open, and when those questions may be answered.

It is subordinate to the frozen Phase 1 contracts and the prospective post-R3
amendment. It may make their meaning more explicit for L02, but it cannot
change the hypothesis, comparison groups, controls, metric relation
vocabulary, boundary ladder, terminal states, or claim ceiling. A conflict
fails closed and requires the controlling contract to reopen before
calibration.

The brief is intentionally not a result, calibration record, registration
record, realization profile, execution plan, or implementation selection. It
prevents the first convenient multi-source representation from quietly
becoming the theory of a shared pool.

`P2-I2-DEC-011` corrects the original singular mode-selection wording. P2-I2
retains `state_carried`, `history_carried`, and `hybrid` as three separately
reviewed and tested causal profiles. Native, minimally producer-assisted, or
missing-prerequisite selection occurs within each mode; no later iteration
chooses one of the three modes as a winner. This correction changes no frozen
Phase 1 hypothesis, cell, control, metric relation, terminal state, or claim
ceiling.

`P2-I2-DEC-012` permits a bounded, separately frozen realization-runtime-
conformance pass before each mode is accepted for calibration. Conformance may
exercise declared native/producer interfaces, interventions, access guards,
restoration, reset, and equal-input continuation. It is quarantined
implementation evidence: it cannot select scientific values, responses,
comparators, delta, seeds, or matrix entries; cannot enter calibration; and
cannot support or falsify L02. A failed conformance classifies realization
inadequacy or a missing prerequisite and reopens that mode before calibration.

Review R3 also constrains how the lane should be implemented. P2-I2 may reuse
the common contract shape and evidence methods proven by P2-I1, but it should
not mechanically reproduce P2-I1's bespoke ceremony. It must keep one
evidence-expandable checklist, one cumulative lane decision record, compact
identity projections, and one lane-local control-resolution index before
terminal closure.

## 2. Source posture

The following sources motivate the search without supplying a P2-I2 result:

| Portable source | Role here | Must not supply |
| --- | --- | --- |
| `rcae:papers/2026-06-FromStateToBecoming.md` | Environment-to-co-created-world transition; coherence economy, cost, and producer-debt method | Runtime evidence, a pool result, or a resource economy |
| `rcae:papers/2026-06-RC-AgenticEcology.md` | Local differentiation, shared support, reserve, leakage, and ecology vocabulary | Native agency, cooperation, collective identity, or an implemented ecology |
| `rcae:papers/2026-06-TheSharedMedium.md` | Medium as common context in which several local activities become mutually relevant | Native shared-medium coordination or collective memory |
| `rcae:papers/2026-06-SharedMediumCoordination-EngineeringSpec.md` | Medium-surface, perturbation, persistence, saturation, hidden-controller, and withdrawal method | Proof that a current LGRC surface is one functional pool |
| `geometric-reflexive-coherence:arc-of-becoming/2026-05-ClassificationOfBecoming.md` | Observation-first classification | Authority to force a predicted class |
| `geometric-reflexive-coherence:arc-of-becoming/2026-05-InterrogationofBecoming.md` | Lowest-honest-boundary discipline | A higher rung than the evidence reaches |
| `geometric-reflexive-coherence:reflexive-coherence-development/2026-06-BecomingAndDevelopment-TwoAxisGuide.md` | Separate what appeared from what condition should be varied next | A predefined developmental answer |
| `geometric-reflexive-coherence:reflexive-coherence-development/2026-06-MeasuresOfReflexiveCoherenceDevelopment.md` | Preserve function, support, capacity, and claim boundaries while measuring signs | Metric authority over interpretation |
| N29 Prototype B and Prototype D records admitted by the [source inventory](../contracts/source-inventory.md) | Separable boundary/medium and medium-reshaping exemplars plus declared composition and leakage debt | Executed multi-source pool composition, native ecology, or resource economy |
| N30 records admitted by the [source inventory](../contracts/source-inventory.md) | Bounded non-private surface perturbation, later eligibility dependence, and causal-control patterns | Multi-source co-conditioning, shared-pool function, communication, or coordination |
| Retained P2-I1 records | A fresh-runtime writer-medium-reader method, source-lineage controls, branch isolation, reconstruction, and bounded interpretation | L02 outcomes, recurrence, delta, fixture bounds, terminal status, or pool evidence |

The inherited graph relation is therefore bounded:

```text
an attributable source can perturb a non-private carrier
and a later response can depend on that changed carrier
```

P2-I2 must add multi-source joint constitution and combined-state dependence.
It cannot receive L02 support by executing the inherited single-source
relation twice.

The source-current graph revision observed during brief preparation,
`3d3d2ef25903d4210a67980f11fdd3ec21e9b6e5`, is the candidate this draft
proposes for P2-I2 source admission. It exposes a native restoration identity:

```text
grc:implementation/Phase-8-LGRC9-RestorationIdentityCloseout.json
pygrc.models.lgrc9v3_restoration_identity_v1
pygrc.models.digest_lgrc9v3_restoration_identity_v1
```

The observed closeout JSON SHA-256 is
`e9799e4b9a877e09ae71d93a4e76288237da219cfc1454821555f14ef7dfa9fe`.
Neither the revision nor this digest is yet an AE01 controlling dependency.
The surface was added after AE01's retained graph-source baseline and must be
admitted by exact revision and digest, then selected through an explicit
realization-profile transition, before P2-I2 consumes it. It supports
versioned restoration identity for PyGRC-owned LGRC9V3 state. It does not
supply a pool, make ecology-owned state native, or replace bounded
continuation replay.

Before selecting a realization, P2-I2 must audit the source-current public
PyGRC surfaces for a native carrier and operations adequate to the L02
discriminator. Native expression is preferred whenever it carries the needed
relation. If native support is absent or inadequate, the smallest explicit
RCAE producer or constructed mechanism may be used, with necessity,
minimality, counterfactual, withdrawal, debt, and graph-side missing-surface
fields preserved. The audit result, not preference or naming, determines the
support classification.

## 3. Core question

P2-I2 asks:

> Can distinguishable perturbations from at least two attributable sources
> jointly constitute one auditable non-private pool state or history so that a
> later continuation depends on the combined shared state rather than on
> private partitions, independent writes, direct messages, contributor labels,
> or a global controller?

The important object is not merely the presence of two records or two
successful source events. It is the causal relation between multiple
contributions, one functional carrier, and a later response to the combined
state or history.

Non-linearity is not required. An additive realization can qualify if the
addition occurs in one declared carrier, both source lineages remain auditable,
the resulting state has one functional identity, and matched controls show
that later continuation depends on that combined state rather than on two
separately consulted source records or a central sum. Conversely, a nonlinear
score produced by a hidden controller does not establish a pool.

The question remains valid when the result is positive, narrow, mixed,
counter-directional, null, blocked, or incomplete. Failure to find an adequate
native pool may still expose a precise missing surface, an unsuitable current
abstraction, or a useful constructed discriminator for later LGRC work.

“Pool” names the ecological search direction. It does not pre-assign resource,
collective-memory, communication, cooperation, or coordination semantics.

## 4. Ecology meaning

L02 keeps four objects distinct:

```text
shared carrier:
  one non-private substrate or explicitly constructed medium surface whose
  access is not exhausted by a private sender-receiver path

attributable contribution:
  a source-bounded perturbation whose lineage, order, amount or condition,
  and cost/support context remain auditable

combined pool state or history:
  one reconstructible carrier condition jointly constituted by at least two
  distinguishable contributions

co-conditioning relation:
  a later continuation whose response depends on that combined pool state or
  history under contributor, shuffle, freeze, and controller controls
```

A shared-pool co-conditioning relation is supported when the registered later
response depends on a jointly constituted carrier condition and the complete
control matrix rejects simpler explanations based on private storage,
independent source success, labels, scheduler state, or centralized
aggregation.

A pool is not reducible to:

- a list containing one row per contributor;
- separate mailboxes under one object name;
- a database table that a controller queries contributor by contributor;
- a global value assembled by a special controller from contributor-addressed
  records and supplied directly to the outcome path without a separately
  intervenable carrier state;
- two P2-I1-style histories that independently cause two later responses;
- co-occurrence without an auditable state/lineage handoff;
- a contributor label or count used as the response condition;
- semantic resource, collective memory, communication, or coordination; or
- a combined score with no independently reconstructible carrier state or
  active history.

One pool need not be spatially uniform, perfectly mixed, conserved, or equally
accessible to every possible participant. It must have one declared carrier
identity and access scope. Local gradients, order effects, saturation, or
bounded partitions may exist only when they are properties of that carrier and
do not become disguised private channels.

Reserve, accumulation, mixing, depletion, saturation, leakage, and maintenance
are not presumed to occur. Each must be measured or assigned an explicit reviewed
`not_applicable` rationale. An inapplicability disposition remains part of
the ecology-structure accounting; it is not permission to omit the question.

The contributors need not communicate, collaborate, or act simultaneously.
The later responder need not represent either source. The minimum ecological
claim concerns how several source histories become mutually relevant through
one shared condition.

## 5. Geometric meaning

The minimal geometric proposition is:

```text
source S1 at time t1 --perturbs--> shared carrier P
source S2 at time t2 --perturbs--> the same carrier P
                                  |
                                  v
               attributable combined state/history H_P(S1,S2[,order])
                                  |
                                  v
                 later opportunity at time t3 > max(t1,t2)
                                  |
                                  v
                    changed registered continuation response
```

The exact source carriers, pool carrier, perturbations, combination rule,
later response, and windows remain open until their declared decision gates.
They may be carried by source-current topology, support, capacity, flux,
causal-pulse surfaces, event history, or another audited PyGRC surface. An
explicitly constructed pool remains admissible only under the native-first and
construction-debt rules below.

For the relation to be geometrically meaningful:

1. Both source contributions must enter the same declared non-private carrier.
2. Each source must remain attributable without using private partitions as
   the claimed pool.
3. The combined carrier state or history must reconstruct independently of a
   success label or contributor-name lookup.
4. Writing, combination, and later encounter must be separated by native
   causal order or an explicitly controlled constructed transition.
5. A later response must depend on the combined mode-relevant carrier state,
   active history, or both under intervention.
6. Contributor deletion, label permutation, pooled-history shuffle, pool
   freeze, and direct/global substitution must distinguish the proposed cause
   from nearby alternatives.
7. Reserve, depletion, leakage, saturation, maintenance, support context, and
   producer ownership must be measured or explicitly classified.
8. The complete branch identity must include every native and ecology-owned
   state component that can affect continuation.

### 5.1 Primitive-to-ecology discriminator gate

The post-R3 amendment requires the brief to bind the following fields before
candidate-blind calibration:

```text
inherited_graph_relation:
  attributable participants can perturb a non-private carrier and later
  eligibility can depend on that changed carrier

ecology_specific_discriminator:
  multiple distinguishable attributable sources jointly constitute one
  functional pool state or history

controlled_ecology_consequence:
  a registered later continuation response depends on the combined pool state
  or history under contributor, shuffle, freeze, and controller controls

insufficient_repetition_case:
  repeated single-source writes, sequential private records, contributor
  labels, or a global aggregate with no combined-state dependence

discriminator_controls:
  each contributor alone; contributor deletion and label permutation;
  pooled-history shuffle; direct-address/global-state substitution; pool
  freeze with contributor activity preserved

claim_ceiling:
  bounded shared-pool co-conditioning demand pattern
```

Reproducing only the inherited graph relation may remain a valid lower-level
observation at its original evidence role. It cannot close P2-I2 as
`supported_bounded_candidate`.

### 5.2 One-pool, causal-factorization, and access gate

“One pool” requires a declared identity boundary:

```text
carrier identity
access scope
state/history identity
source-lineage relation
write rule
read or susceptibility rule
persistence window
leakage boundary
```

The same object name, Python container, graph region, or serialized file does
not establish one pool. The realization must show which state is common and
which source-specific metadata is retained only for attribution. A later
response must consult or encounter the common state, not choose among private
source slots.

If safe attribution requires retaining source-local records, the brief does
not forbid them. The registered pool projection must demonstrate that those
records jointly determine one carrier condition and that the later response
does not bypass that condition by reading the records independently.

The minimum causal factorization is:

```text
common carrier transition:
  C_P(k+1) = U(C_P(k), q(k), local_context(k))

audit lineage:
  L(k+1) = audit_append(L(k), source_id(k), q(k), time(k))

later continuation:
  Y = V(C_P(t3), opportunity_context)

forbidden bypass:
  Y = G(L, contributor_slots, contributor_count, success_labels)
```

`C_P` contains encounter state, active carrier history, or both according to
the applicable retained dependence-mode profile. `q(k)` may include causal contribution
properties such as amount, location, timing, or registered contribution type.
An arbitrary source label may remain in `L`; it must not enter `V`. This
factorization constrains causal interfaces, not code location. A centrally
executed native transition or a carrier-owned scalar may qualify when the
carrier is independently intervenable and the response encounters it through
the declared carrier path. Centralized execution alone is not the forbidden
controller case.

The registration must make both directions falsifiable:

1. Hold the complete causal carrier state and active history fixed and alter
   attribution-only metadata. The later response must remain invariant under
   the frozen resolution/equality rule.
2. Hold attribution metadata, contributor activity, and unrelated opportunity
   conditions fixed while altering, blocking, or clamping the mode-relevant
   common carrier component. The registered response relation must change or
   the candidate effect must disappear. Otherwise combined-pool causal
   dependence is unsupported.

R01 also requires a non-private access witness. A later response must encounter
the carrier through its location, topology, or common eligibility, without a
source address. Access must follow the declared pool scope rather than
contributor identity. A responder may itself be a contributor, but its read or
susceptibility path must be the same carrier-scoped path available to another
eligible participant.

A retained audit or lineage log is not a functional pool merely because a
controller can query it. An event-history realization qualifies only when the
history is active runtime state encountered by the continuation mechanism and
is independently intervenable from its audit projection.

### 5.3 Combined-state or combined-history dependence gate

Joint dependence is functional rather than merely representational:

```text
combined carrier response
  differs from the registered nearest mode-appropriate primary comparator
  because the later opportunity encounters P_combined(t3), active H_P, or both
```

Mode-specific controls expected to be invariant do not become positive-margin
primary comparators. A state-preserving shuffle, for example, tests the
`state_carried` interpretation but cannot own a preregistered contrast that
requires divergence.

Both single-source responses must remain visible. They may be zero, partial,
aligned, counter-directional, or individually sufficient for a lower-level
effect. The lane does not require an AND gate or synergy. It requires evidence
that the claimed combined response is carried by one state/history whose
causal identity changes when contribution structure is altered.

A derived interaction, synergy, or order term may be retained as a secondary
diagnostic. It cannot replace the frozen primary normalized margin or the
causal controls.

Before calibration, the lane must retain and separately preregister all three
dependence modes:

```text
pool_dependence_mode:
  state_carried
  history_carried
  hybrid
```

The modes determine control semantics rather than claim strength and are not
ranked against one another:

- `state_carried`: the complete common state at encounter time is causal.
  Order or shuffle variants that preserve that state should preserve the later
  response. A discriminator must instead alter the state, prevent common
  constitution, partition the writes, or bypass the carrier.
- `history_carried`: ordered or temporally structured history is part of the
  carrier identity. A registered shuffle that preserves marginal quantities
  but changes that history should change the later response.
- `hybrid`: both encounter state and ordered history are causal. Registration
  must identify which interventions alter each component and separate
  state-matched history variants from history-matched state variants where the
  realization permits.

`hybrid` is admissible only when encounter state and active history are
separately declared, each has at least one intervention that changes it while
matching the other component as closely as the realization permits, and the
expected response relation for each intervention is frozen. Failure to obtain
the required separation closes lower or mixed; it cannot trigger a
post-outcome mode change.

The stable metric name `combined_pool_history_normalized_margin` does not force
a history-carried realization. For a state-carried realization, “history” in
that frozen identifier denotes the multi-contribution provenance of the
candidate state, not a requirement that the responder read temporal order.

At least one jointness counterfactual must preserve the inherited
single-source carrier relation while removing joint pool constitution. If the
registered combined consequence remains fully explained by that preserved
single-source relation, L02 cannot close as `supported_bounded_candidate`.

### 5.4 Attribution, order, and mixing gate

Attribution and pooling must coexist:

- source lineage identifies which perturbations entered the carrier;
- source-label permutation tests whether labels rather than source-relative
  contribution structure carry the response;
- at least two combined orders are frozen when order can matter;
- an order-invariant realization must justify why order is structurally
  inapplicable and still test a mode-appropriate state, constitution, or
  pooled-history discriminator;
- shuffling preserves registered marginal contribution quantity while
  producing the invariant or divergent relation declared by the selected
  dependence mode; and
- reconstruction verifies the complete source-to-pool-to-response chain.

“Mixing” means the registered rule by which contributions jointly affect the
carrier. It need not mean physical homogenization. The rule may preserve order,
locality, or source-relative structure, but it must not reduce the pool to a
dispatcher over private records.

### 5.5 Native, producer, and restoration gate

The realization search follows this order:

```text
source-current public PyGRC surface adequate
  -> use the native surface and retain its exact claim boundary

native surface present but inadequate for the L02 discriminator
  -> record the unsuitable distinction and add only the minimal controlled
     producer or ecology-owned state needed to expose the missing relation

native surface absent
  -> construct the smallest honest pool probe or close as a classified missing
     prerequisite when no bounded construction can preserve the discriminator
```

A producer-backed pool is scientifically useful when it exposes a controlled
ecology relation and a precise graph-side demand. It remains producer-assisted
or constructed. If one producer creates the pool, reads it, and authors the
later success outcome without separable interventions, the result is
producer-carried and cannot satisfy the strong L02 boundary.

When the source-current helper is admitted and selected, native branch
restoration identity is:

```text
identity document =
  pygrc.models.lgrc9v3_restoration_identity_v1(model_or_snapshot)

identity digest =
  pygrc.models.digest_lgrc9v3_restoration_identity_v1(model_or_snapshot)
```

The complete P2-I2 branch identity is compositional:

```text
P2-I2 branch identity
  = versioned PyGRC restoration identity
  + ecology-owned pool state not already inside PyGRC identity
  + intervention and producer configuration identity
```

Raw snapshot digests remain retained observations. Restoration-identity
equality is necessary but does not replace equal-input continuation. If the
pool is native and already included in the PyGRC runtime artifact, the
registration must show that coverage rather than double-count it as external
state. Silent transition from P2-I1's RCAE projection is forbidden.

## 6. Observation model

The frozen primary metric is the L02 combined-pool-history normalized margin:

```text
combined_pool_history_response - matched_comparator_response
--------------------------------------------------------------------------
max(
  abs(combined_pool_history_response),
  abs(matched_comparator_response),
  measurement_resolution
)
```

The exact raw later-continuation response is not selected by this brief. It
must be chosen and justified before candidate-blind calibration, then remain
identical across calibration and live analysis. The response must be a
substrate-visible or explicitly bounded constructed consequence of encountering
the pool, not a post-hoc pool-presence label or authored success field.

The metric sheet permits a matched comparator drawn from the single-source,
shuffled-history, contributor-removal, or controller-exclusion structure. The
lane must freeze one exact primary candidate/comparator pairing before
calibration. Other comparisons remain mandatory causal diagnostics and cannot
be searched after execution for the largest margin.

The selected primary comparator must instantiate the closest registered
insufficient-repetition explanation for the chosen realization, not merely a
permitted comparator expected to yield a large margin. The preregistration
must state why it is the nearest causal alternative. If several alternatives
are comparably close, one remains primary and the others remain signed
mandatory controls; outcome magnitude cannot select among them.

The registration bundle must freeze an orientation transform before candidate
outcomes exist:

```text
oriented response =  raw response   when higher is the aligned direction
oriented response = -raw response   when lower is the aligned direction
```

Per-seed margins and every raw single-source, combined-order, shuffle, label,
removal, freeze/clamp, private-partition, controller-exclusion,
quantity-matched, and transfer-contrast response remain visible.
The strict zero threshold and candidate-blind resolution band describe
direction and resolution; they do not decide the terminal class. A robust
margin can still be mailbox-carried, controller-produced, leakage-dependent,
or fixture-locked. A narrow, mixed, or counter-directional relation may still
disclose a useful pool boundary or missing native distinction.

The primary claim is distributional. A single combined event or one successful
order cannot establish a shared-pool possibility structure.

## 7. Minimum, strong, and maximum within L02

These levels summarize the frozen L02 boundary ladder. They are not a
replacement score and remain orthogonal to metric relation, support status,
classification value, realization class, and terminal state.

| Level | Frozen rungs | Geometric meaning | Ecology meaning | Stronger claim still blocked |
| --- | --- | --- | --- | --- |
| Prerequisite or classified absence | Below `AE01-L02-R01` or blocked | A non-private carrier, access scope, or reconstructible state cannot be represented | A precise missing/unsuitable pool condition | Any shared-pool result |
| Minimum interpretable observation | R01–R02 | One non-private pool receives multiple attributable source contributions | A multi-source shared-carrier observation worth classifying | Combined-state or active-history function |
| Strong bounded result | R01–R04 | Later response depends on combined pool state or history and survives mailbox, controller, mode-appropriate shuffle, deletion, and freeze controls | Bounded shared-pool co-conditioning demand pattern within the registered support | Transfer, native pool organization, communication, coordination |
| Maximum result in P2-I2 | R01–R05 | The controlled relation remains under one frozen capacity, contributor, or access-scope variation | Limited transfer of the bounded pool demand pattern | General pooling, collective memory, resource economy, native coordination, motif, or regime |

The exact rung names are:

```text
AE01-L02-R01 = One auditable non-private pool
AE01-L02-R02 = Attributable contributions from multiple sources
AE01-L02-R03 = Later response depends on combined pool history
AE01-L02-R04 = Mailbox, controller, shuffle, and contributor controls preserve specificity
AE01-L02-R05 = Capacity, contributor, or access variation retains the shared-pool function
```

The R03 name is a frozen inherited identifier. For a `state_carried`
realization, “combined pool history” is interpreted mode-aware as the
multi-contribution provenance of the complete common encounter state; it does
not require order-sensitive response behavior.

For this lane:

```text
bounded shared-pool co-conditioning candidate:
  the controlled ecology-side relation observed within the registered scope

bounded shared-pool co-conditioning demand pattern:
  that relation plus the explicit substrate distinction, support, or reusable
  mechanism that later LGRC work would need to carry more natively
```

Reaching R05 does not make constructed support native or assign cross-lane
recurrence.

## 8. Frozen comparison structure

P2-I2 must materialize the seven Phase 1 logical cells without adding rescue
variants after candidate observation:

| Cell | Role in the inquiry |
| --- | --- |
| `reference-pool` | Declare carrier identity, access scope, baseline state/history, support, reserve, leakage, maintenance, and later response without the candidate combined state or active history. |
| `individual-contributions` | Execute and preserve each attributable source contribution separately under matched opportunity and support. |
| `combined-orders` | Constitute the candidate pool from all registered contributions in at least two frozen orders when order can matter, preserving each raw order response. |
| `pooled-history-shuffle` | Preserve registered marginal contribution quantity and exercise each applicable dependence-mode profile: preserve complete state for an expected state-carried invariance, alter carrier history for a history-carried divergence, or separate both components for a hybrid candidate. |
| `contributor-removal` | Remove one contributor or contribution while preserving the remaining source activity, pool opportunity, and later-response conditions; pair with source-label permutation. |
| `global-state-exclusion` | Materialize the private-partition competitor and substitute or expose the nearest direct-address, scheduler, or controller-assembled global explanation; verify that none can satisfy the pool-specific causal chain. |
| `access-capacity-contrast` | Vary one frozen capacity, contributor, or access-scope axis while preserving the causal question and recording budget/leakage consequences. |

The lane-specific controls are:

```text
AE01-L02-CTRL-01 = each contributor alone
AE01-L02-CTRL-02 = contributor deletion and label permutation
AE01-L02-CTRL-03 = pooled-history shuffle
AE01-L02-CTRL-04 = direct-address/global-state substitution
AE01-L02-CTRL-05 = pool freeze with contributor activity preserved
```

The private-partition subconfiguration is explicit:

```text
same marginal source contributions
same timing, support, opportunity, and access opportunity
source-specific carrier partitions
no jointly constituted common state
no responder bypass that independently consults both partitions
```

It may live inside `global-state-exclusion` or in a separately linked control
artifact. It does not add an eighth logical cell. Reproducing the candidate
through independent reads of both partitions is a mailbox result, not a pool
result.

Before calibration preregistration, every control and subconfiguration must
freeze:

```text
intervention_target
variables_held_fixed
expected_invariance_or_divergence
allowed_ambiguous_outcome
fail_closed_classification_effect
```

The minimum signed semantics are:

| Control or diagnostic | Target and held-fixed relation | Expected relation | Ambiguity and fail-closed effect |
| --- | --- | --- | --- |
| Each contributor alone | Remove all other contributions; hold opportunity, support, and response measurement fixed. | Preserve every raw response; the complete combined relation must not be fully explained by a retained single-source relation. | Mixed or individually sufficient effects remain observations; full single-source explanation blocks R03 support. |
| Pure source-label permutation | Change attribution-only labels while holding complete pool state and contribution physics fixed. | Pool state and later response remain invariant within the frozen rule. | Non-invariance exposes a label read path and blocks R04. |
| Contribution-operation reassignment | Change amount, location, timing, or contribution type rather than an arbitrary label. | Divergence is permitted and must not be reported as failure of pure label invariance. | Conflating this intervention with label permutation invalidates the label control. |
| Contributor deletion | Remove one contribution while holding scheduling, opportunity, unrelated activity, and remaining contribution conditions fixed. | The registered joint constitution and its response relation change as preregistered. | An unchanged fully explained relation blocks the affected R02/R03 claim; instability may close lower or mixed. |
| Shuffle or order | Preserve marginal quantities and apply the registered state-, history-, or hybrid-mode intervention. | State-preserving invariance for `state_carried`; history-sensitive divergence for `history_carried`; component-specific relations for `hybrid`. | A relation inconsistent with the selected mode blocks R03 or forces a lower/mixed classification; the mode cannot be changed after outcomes. |
| Pool-write freeze | Preserve contributor activity while preventing candidate pool change and holding the pool at the registered reference condition. | The candidate response relation disappears or follows the frozen reference relation. | Persistence or clamp ambiguity must be declared in advance; failure to separate the relation blocks R03/R04. |
| Candidate-state clamp or active-history analogue | Hold contributor activity, lineage, and opportunity fixed while clamping the mode-relevant carrier component to registered reference and candidate conditions. | The response tracks the clamped common state or active history under the frozen relation. | No clamp-dependent separation blocks the corresponding common-carrier causal dependence. |
| Private-partition substitution | Preserve marginal writes, timing, support, and access opportunity while replacing common state with source-specific partitions. | The claimed pool relation is not reproduced through a legitimate carrier-scoped path. | Reproduction by consulting both partitions is mailbox dependence and blocks R04. |
| Direct/global substitution | Bypass the carrier with direct addressing or a controller-assembled value while preserving the nearest output opportunity. | A substitution may reproduce an output, but it must fail the pool-specific source-to-carrier-to-response chain. | If the candidate cannot be causally distinguished from the bypass, R04 remains blocked. |
| Quantity-matched single-source replacement | One contributor supplies the combined registered quantity while support, encounter state, and opportunity remain matched where possible. | Either relation is allowed; this is a scope diagnostic, not an automatic support gate. | Equivalence limits the claim to several contributors populating a common pool; divergence may support a bounded contribution-structure statement, never cooperation or coordination. |

The concrete allocation of label permutation and pool freeze into cell
subconfigurations or separate control artifacts must be frozen before
candidate execution. They are mandatory controls even though the Phase 1
matrix names seven logical cells rather than one cell per intervention.

All applicable common controls remain required. Any `not_applicable`
disposition requires a control-specific reviewed rationale and must appear in
the compact lane-local control-resolution index.

## 9. Anti-subsumption boundary

L02 owns multi-source constitution of one functional pool. It must not absorb
another lane merely because that lane also uses several activities or shared
state.

| Other lane | Defining causal distinction | When an L02 observation should redirect rather than subsume |
| --- | --- | --- |
| L01 — niche conditioning | One attributable history differentially alters later participant-relative possibility | The result is fully explained by one source history or by reader susceptibility rather than joint pool constitution. |
| L03 — trail/stigmergic field | Costly repeated deposition changes decay, reinforcement, saturation, or maintenance dynamics that alter traversal | Trace dynamics and route function, rather than multi-source combined state, carry the result. |
| L04 — nursery/support field | Produced, depleted, or maintained support differentially governs fragile versus robust local formation | Support provisioning and fragility are the causal center. |
| L05 — boundary-conditioned exchange | Maintained selective coupling controls exchange, transformation, leakage, repair, and later closure | Interface state and permeability carry the effect. |
| L06 — capacity circulation | An audited depletion/replenishment or return cycle closes a capacity budget and preserves floors | Circulation and budget closure, not pool constitution, explain the later response. |
| L07 — parent-basin modulation demand | A parent/local causal distinction is required but absent or unsuitable | The effect depends on parent-wide modulation, copied parameters, or a missing graph distinction. |

Multiple participants responding to one pool may be recorded as an adjacent
observation, but L02 cannot call that coordination or co-response without a
separately controlled relation and a permissible claim ceiling. A redirect
does not erase the L02 observation or move its evidence into another lane. It
becomes a bounded next move or a P2-I8 synthesis input with independent lineage
preserved.

## 10. What P2-I2 may reuse

P2-I2 may reuse the following established P2-I1 and Phase 1 methods after
checking them against its own contracts:

- canonical JSON, semantic digest, portable-path, schema, and manifest tooling;
- candidate-blind matched-null calibration and frozen metric-sheet procedure;
- explicit registration evidence and cycle-scoped execution-freeze pattern;
- source-current runtime binding with graph read-only guards and no fallback;
- participant/source versus medium separation or co-constitution template;
- attributable source-lineage and native causal-order recording;
- state-matched withdrawal, shuffle, substitution, and support-control logic;
- fresh-worker isolation and independently restored counterfactual branches;
- raw snapshot diagnostics plus bounded equal-input continuation checks;
- per-seed distribution, boundary/support/classification-value interpretation;
  and
- blocked-claim, debt, missing-surface, and falsifiable-next-move report shape.

When the admitted realization is compatible and the profile selects it,
P2-I2 may adopt PyGRC's native versioned restoration identity instead of
copying P2-I1's RCAE-owned C02 restoration projection. The native-first audit
must record that disposition; adoption is not unconditional for every
realization. The transition must be explicit, and ecology-owned pool and
intervention state remain separately composed.

P2-I2 may not reuse an L01 conclusion, delta, threshold result, fixture,
response panel, reader profile, participant identity, source digest,
realization status, rung, support classification, or terminal classification
as evidence. It must perform its own source audit, calibration, registration,
execution, controls, reconstruction, interpretation, and closure.

R3 specifically prohibits copying ceremony without a concrete evidence need.
P2-I2 should generate compact identity and control projections from retained
evidence and introduce a new record shape only when an actual semantic or
auditability gap requires it.

## 11. What must be fixed before calibration and execution

The lane-local D-039 binding must be owner-accepted before candidate-blind
calibration. At minimum, the calibration preregistration must freeze:

- the inherited graph relation, ecology discriminator, controlled consequence,
  insufficient-repetition case, discriminator controls, and claim ceiling
  stated in Section 5.1;
- each retained mode's selected realization class, candidate pool meaning,
  dependence semantics, and operational test for one carrier;
- the causal factorization, attribution-only invariance, common-state
  intervention, non-private access witness, and audit-log exclusion;
- each mode's exact raw later-continuation response and orientation, with any
  shared definition explicitly justified rather than assumed;
- one primary combined-state-or-history/comparator pairing per mode and each
  nearest-insufficient-alternative rationale;
- the aggregation and paired-margin functions shared by calibration and live
  analysis;
- the candidate-blind matched-null generator, inputs, seeds, and proof of
  candidate/runtime exclusion;
- the signed semantics and fail-closed effects for every raw single-source,
  order/shuffle, removal, label, freeze/clamp, private-partition,
  direct/controller, quantity-matched, and transfer response; and
- the stopping rule that prevents inherited single-source repetition from
  satisfying L02.

Before candidate execution, the registration evidence bundle must additionally
declare:

- exact source carriers, source continuity criteria, and attribution fields
  for every retained mode;
- each mode's exact pool carrier, identity boundary, access scope, and
  persistence window;
- source-current native-surface audit and adequacy disposition;
- exact contribution operations, amounts or conditions, ordering, timing,
  costs, and support contexts;
- combination/mixing rule and the actual runtime mapping of common state,
  active carrier history, and audit-only attribution metadata;
- reserve, accumulation, depletion, saturation, leakage, and maintenance
  measurements or reviewed inapplicability dispositions;
- native, producer-assisted, and ecology-owned state/transition boundaries;
- constructed-mechanism necessity, minimality, counterfactual, withdrawal,
  debt, and graph-discriminator fields when applicable;
- explicit transition to the admitted versioned PyGRC restoration provider,
  plus complete composite branch identity and continuation procedure;
- exact realization of all seven logical cells and five L02 controls for every
  retained mode, including the private-partition, factorization, clamp,
  jointness, and quantity-matched subconfigurations; and
- lane-specific claim, support, debt, and stopping boundaries.

The dedicated P2-I2 checklist will own common seeds, attempt/resource limits,
worker isolation, artifacts, manifests, reconstruction, validation, compact
indexes, and closure evidence by reference to the existing common contracts.
This brief retains their gates without duplicating the full completion
ceremony. None of these declarations answers whether a shared pool occurs.

Before calibration, the checklist may additionally require a mode-specific
runtime-conformance fixture under DEC-012. That fixture must be frozen before
its first runtime call, have a finite no-search run count, use identities
separate from calibration/registration/candidate execution, and retain an
explicit no-scientific-evidence receipt. It does not replace I04
preregistration, I05 calibration, I06 registration, I07 authorization, or I08
execution.

## 12. Open questions and decision timing

Unknowns are first-class iteration content. They must be answered only when
the relevant evidence or decision basis exists.

| Open question | Earliest legitimate decision point | If unresolved |
| --- | --- | --- |
| Which source-current PyGRC surfaces could carry one pool? | Native-surface audit before calibration preregistration | Do not select a realization or infer absence from naming alone. |
| Is a candidate native surface adequate to joint constitution and combined-state dependence? | Native-adequacy decision before calibration | Record the precise unsuitable/missing distinction; select a minimal producer path or classify the prerequisite. |
| What are the two or more distinguishable source carriers or lineages? | Calibration preregistration concept; exact IDs at registration | Multi-source attribution and L02-R02 remain blocked. |
| What makes the carrier one pool rather than one container holding private records? | Calibration preregistration, verified by registration and controls | Candidate execution remains blocked. |
| How are `state_carried`, `history_carried`, and `hybrid` each realized without selecting among them? | Review-separated I03A–I03C profiles; calibration preregistration preserves all three | Any unbound mode retains a reviewed missing-prerequisite disposition; shuffle, order, clamp, and primary-comparator semantics remain undefined until its profile is bound. |
| What exact combination or mixing rule constitutes pool state or active history? | Calibration preregistration | The primary metric and mode-appropriate controls are undefined. |
| Which one later-continuation response is primary? | Calibration preregistration | Delta and candidate execution remain blocked; do not search across outcomes. |
| Which nearest insufficient-repetition comparator owns the primary normalized margin? | Calibration preregistration | The metric remains ambiguous; expected margin size cannot choose the comparator. |
| Does order matter, and which two orders are meaningful? | Calibration preregistration with source-current rationale | Freeze two meaningful orders or a reviewed order-inapplicability plus a mode-appropriate state or constitution discriminator. |
| How is attribution retained without entering the response read path? | Calibration preregistration and registration factorization | Downgrade, redesign, or classify missing prerequisite. |
| What witnesses carrier-scoped non-private access without a source address? | Calibration preregistration concept; exact path at registration | R01 and candidate execution remain blocked. |
| How are reserve, accumulation, mixing, depletion, saturation, leakage, and maintenance observed? | Registration | Measure each or retain an explicit applicability/debt disposition. |
| What producer state is minimally necessary if native support is inadequate? | Constructed-mechanism decision before registration | No undeclared producer transition may enter the run. |
| Does a producer compute both pool state and later success? | Construction review and withdrawal design | Treat the relation as producer-carried unless separable intervention proves otherwise. |
| Does native restoration identity cover each retained mode's selected pool state and active history? | Realization-profile transition and registration | Compose external identity explicitly per mode or block that mode's branching; never assume coverage. |
| What private-partition, direct-address, global-state, and scheduler alternatives are closest? | Calibration preregistration; exact subconfigurations at registration | Global/private exclusion is not testable and L02-R04 remains blocked. |
| Which capacity, contributor, or access-scope axis is the one transfer contrast? | Registration | R05 remains unavailable; do not search across variants. |
| What support or parent context carries the relation? | Registration and control completion | Preserve support dependence and block parent/ecology relabels. |
| Does the observed geometry justify “shared pool”? | Terminal interpretation | Retain a lower shared-carrier or multi-source observation, or redescribe the aim. |
| Does the missing native relation recur elsewhere? | P2-I8 after independent lane closure | No recurrence or graph-implementation priority may be assigned in P2-I2. |

## 13. Result interpretation

Every completed result is read along both axes:

**Becoming reading:** What common carrier, attributable contributions,
combined state/history, and later functional dependence appeared? At which
rung, under which native or constructed support, and with which stronger claim
blocked?

**Development reading:** What native capability, producer transition, support
condition, leakage boundary, or missing distinction produced or prevented the
relation? Should the next move retain, vary, withdraw, substitute, redesign,
redirect, redescribe, or stop?

The common completion contract and dedicated checklist own exhaustive result
retention. This brief additionally requires the interpretation to preserve:

- all three retained dependence modes and every mode-specific control
  relation, including any reviewed missing-prerequisite disposition;
- the common-state/audit-lineage factorization, access witness, and
  private-partition result;
- exact native, producer-assisted, constructed, and ecology-owned state roles,
  including restoration and equal-input continuation boundaries;
- the observed relation separately from its LGRC demand implication; and
- the strongest valid claim, blocked relabels, and one next move with a
  falsifier.

No optimization may target the normalized margin itself. A local refinement
must target the disclosed pool function or missing distinction and become a
new preregistration under the D-038 guard.

## 14. Iteration outputs

The lane-specific output spine is:

1. an owner-accepted version of this brief;
2. one detailed evidence-expandable P2-I2 checklist and one cumulative lane
   decision record;
3. a source-current PyGRC capability audit and explicit restoration-profile
   transition or classified fallback;
4. frozen mode-indexed calibration and registration evidence for all three
   retained realizations or reviewed mode-specific missing prerequisites;
5. the L02 pattern/surface and any constructed-mechanism records, with a
   compact lane-local control-resolution index; and
6. one reconstructed terminal classification and developmental interpretation
   feeding the existing demand, missing-surface, debt, and future graph-
   discriminator projections.

The checklist, not this brief, expands those outputs into manifests, receipts,
validation, reports, and common record obligations.

None of these outputs is presumed to be positive. No output may assign cross-
lane recurrence or select N31+.

## 15. Exit boundary

P2-I2 closes only when the frozen attempt matrix has one complete terminal
classification, every mandatory common and L02 control resolves through the
compact index, retained evidence reconstructs, native and producer ownership
are explicit, and the developmental interpretation records the lowest honest
boundary and one bounded next move. The one lane-level terminal classification
must preserve separate state-carried, history-carried, and hybrid realization,
execution, control, support, and developmental dispositions; it may not select
or collapse them by convenience or observed outcome.

The maximum handoff is a bounded shared-pool co-conditioning demand pattern
with its native/constructed support, contribution structure, access scope,
leakage, transfer, restoration, debt, and claim boundaries intact.

P2-I2 cannot establish collective memory, semantic communication, resource
economy, cooperation, coordination, agency, organism identity, a reusable
native pool primitive, ecology motif, ecology regime, life, cross-lane
recurrence, or N31+ selection. A precise missing or unsuitable LGRC surface is
an admissible and useful result, but it remains ecology-side demand until a
later graph experiment tests it under graph-side authority.

## Appendix A — Grounded three-operation shared-carrier composition extension

**Status:** owner-accepted and closed under DEC-074/CHG-079; APP-A2 establishes
the bounded positive result at the A.5 claim ceiling. This appendix does not
reopen, revise, or strengthen the accepted I11 result or any passed main P2-I2
gate.

### A.1 Exact question and evidence boundary

Appendix A asks one narrower extension question:

> Can physically distinct generative enrichment (`G`), leakage-gated
> extraction (`E`), and processor redistribution/buffering (`P`) operations
> compose with subset-bounded irreducibility through one non-private live
> carrier, relative to one frozen gate signature, so that a later response
> (`R`) depends on the complete operation composition rather than on any valid
> proper subset?

This question is grounded in retained source relations; it is not already
answered by them:

| Appendix role | Portable source | Bound meaning | Unresolved here |
| --- | --- | --- | --- |
| `G` | `grc:experiments/2026-06-N29-lgrc-agentic-ecology-convergence-bridge/outputs/n29_generative_enrichment_runtime_i141.json` | generative enrichment changes the phase-aligned medium | contribution inside one three-operation live carrier |
| `E` | `grc:experiments/2026-06-N29-lgrc-agentic-ecology-convergence-bridge/outputs/n29_extractive_clean_constructed_runtime_i1423.json` | leakage-gated extraction depletes the generator-changed medium | contribution inside the complete composition |
| `P` | `grc:experiments/2026-06-N29-lgrc-agentic-ecology-convergence-bridge/outputs/n29_processor_redistribution_runtime_i143.json` | processor redistribution buffers the extractor-changed medium | irreducible contribution rather than generic stabilization |
| `R` | `grc:experiments/2026-06-N29-lgrc-agentic-ecology-convergence-bridge/outputs/n29_buffered_generator_extractor_feedback_i1452_artifact.json` | later generator consumes the buffered changed medium | later response from a live three-operation carrier |

N29 I14.5-2 is producer-mediated bridge evidence with replay and stress
support. It is not native composition or three-participant evidence. N30
grounds only the bounded changed-medium to later-eligibility relation. The
accepted main P2-I2 campaign grounds shared-carrier factorization,
attribution, intervention, restoration, fresh-entry isolation, reconstruction,
and bounded claim discipline. Appendix A adds the complete live composition
test rather than inferring it from those artifacts.

The primary source-backed realization is hybrid: both the changed carrier and
the ordered operation handoff are load-bearing. The accepted P2-I2 state-only
and history-only interventions are reused as decomposition controls, not
rerun as three full candidate campaigns. This does not rank or reclassify the
three main P2-I2 modes.

### A.2 Fresh-runtime requirement

Artifact manipulation is not Appendix A evidence. Each scientific arm must:

- start in a fresh process from one exact registered model and baseline;
- construct or restore one live non-private carrier through source-current
  PyGRC;
- execute the arm's selected `G`, `E`, and `P` operations against that live
  carrier in the frozen order;
- retain operation-level receipts showing that each selected operation was
  invoked through the registered live-carrier path rather than against a
  detached analysis object, while recording a positive, negative, or exactly
  zero carrier delta;
- prove one unbroken carrier lineage: each operation receives the exact carrier
  identity emitted by its predecessor, and `R` receives the exact final
  identity, with no copied, substituted, or controller-computed carrier;
- execute the later `R` response from the resulting live carrier state and
  history;
- retain raw before/after carrier identities, event/packet traces, response
  records, intervention receipts, and post-run restoration identities; and
- reconstruct the evidence and conclusion deterministically from the retained
  runtime records.

Replay-only evidence, arithmetic over N29 or P2-I2 outputs, synthetic assembly
of expected envelopes, and report generation cannot satisfy the hypothesis.
They may validate provenance or reconstruction only.

Native PyGRC is preferred for every load-bearing transition. If native
machinery is absent or inadequate, APP-A1 may freeze the smallest explicit
producer that drives the missing live transition. Its state, call path,
withdrawal, restoration, and necessity must remain observable. A producer
that merely calculates an expected value or assembles the final response is
not an admissible realization.

### A.3 Complete-subset test and causal controls

The primary fresh-runtime matrix is fixed as:

```text
reference
G
E
P
G+E
G+P
E+P
G+E+P
```

The primary contributors are physically distinct `S_G`, `S_E`, and `S_P`;
`R` is a later noncontributor with carrier-scoped access. Each missing-
operation participant remains active and pays the registered cost while its
operation is diverted from the carrier through a verified no-influence path.
Timing, opportunity, and operation slots remain matched. Every single and pair
arm must be operationally valid; an incomplete proper-subset arm makes the
full tuple nonevaluable. Operational validity requires the registered live-
carrier call, matched opportunity/cost/timing/path, and complete receipts. It
does not require a nonzero effect. For example, valid `E`-only extraction or
`P`-only redistribution may produce exactly zero because the prerequisite
carrier condition is absent; that zero is a scientific result, not an invalid
arm. The reference arm is valid only when the exact baseline is restored, all
operation routes are verifiably diverted, and matched participant activity
and receipts remain present.

There is no scalar maximum across heterogeneous gates and no outcome-selected
strongest pair. `GE`, `GP`, and `EP` are the nearest comparison family and are
considered jointly. APP-A1 must freeze this signature for every arm:

```text
GateSignature(X) = {
  environment_feedback_gate,
  support_feedback_gate,
  phase_residual_gate,
  merge_leakage_gate,
  retention_configuration_condition
}
```

The first four members are measured functional gates. The retention condition
is the fixed fixture identity `buffered_feedback_retention_factor = 0.82`; it
is not an independent measured success observation. Every arm must satisfy
that same condition; a mismatch makes the arm nonevaluable and cannot count as
proper-subset failure.

The exact composition relation requires:

```text
GEP satisfies the fixed retention condition and passes all four measured gates
and
every valid proper subset in {reference, G, E, P, GE, GP, EP}
satisfies the same retention condition but fails at least one of the same four
measured gates
and
for each X in {G, E, P}, withdrawing X either changes R directly
or changes a registered mediator whose independent intervention changes R
and
freezing or clamping the common carrier removes or changes R
```

Direct and mediated paths must be derived from runtime receipts, not authored
causal booleans. The resulting property is named only **subset-bounded
irreducibility relative to the frozen gate signature**. It does not imply
general or out-of-fixture irreducibility.

The candidate N29 I14.5-2 measured gates are:

```text
later environment feedback delta >= 0.04
later support feedback delta >= 0.02
maximum phase residual <= 0.015
merge leakage <= 0.025
```

APP-A1 may reuse those values as pass/fail authority only after comparing the
raw metric definitions, units/orientation, reference and candidate meanings,
temporal windows, normalization and denominator rules, topology/scaling,
leakage accounting, phase-residual construction, response target, and runtime
numeric domain. Reuse requires:

```text
metric_semantics_equivalent = true
AND
measurement_domain_equivalent = true
```

If either is false, the N29 numbers remain context or diagnostics only and
APP-A1 fails its execution-freeze boundary closed until separately reviewed
Appendix A gate authority exists. If both are true, APP-A1 binds the exact
machine projection and runtime tolerance once without search. Appendix A
performs no matched-null or threshold-search campaign.

One capacity-relaxed source must also perform the same `G -> E -> P`
operations. If it reproduces the complete relation, operation composition may
remain supported while physical participant plurality is classified as non-
load-bearing. If it does not reproduce the relation, participant plurality
remains unresolved unless APP-A1 proves matched capacity, access, operation
authority, timing, initialization, and cost and freezes at least one complete-
`GEP` contributor-to-operation role rotation. Any resulting plurality
classification also requires that APP-A2 execute that rotation and reproduce
the complete relation and operation-to-`R` paths; it remains bounded to that
fixture. Source-label permutation guards against label causation; the role
rotation guards against fixed participant identity. Full six-way rotation is
unnecessary.

The order diagnostics are the two adjacent inversions:

```text
E -> G -> P
G -> P -> E
```

Only if both valid inversions lose the complete relation may the bounded full
`G -> E -> P` sequence be called load-bearing. If only one does, the result is
limited to at least one source-grounded handoff being order-sensitive.
Private partitions, direct contributor-addressed reads, controller-computed
aggregates, repeated same-operation quantity, carrier freeze/clamp, and the
two adjacent inversions remain bounded controls or scope diagnostics.

### A.4 Compact evidence cycle

Appendix A builds on accepted P2-I2 machinery and therefore uses only:

```text
APP-A0 = source/semantic/hypothesis binding and owner review
APP-A1 = source/delta audit
         -> native/producer realization selection
         -> metric-semantic and measurement-domain equivalence decision
         -> exact fixture, gate signature, and controls
         -> candidate-free conformance if new machinery is introduced
         -> execution freeze
         -> owner review
APP-A2 = one bounded fresh-runtime matrix + deterministic reconstruction/
         control resolution + one closeout conclusion
```

This removes repeated ceremony, not runtime evidence. There is no new I04
arithmetic-null campaign, no separate calibration search, and no split I09,
I10, or I11 closeout sequence. Interfaces reused without changed scientific
meaning remain bound to accepted P2-I2 machinery; all Appendix A deltas are
made explicit once in APP-A1. APP-A2 produces one cumulative runtime evidence
and closeout package and then stops for owner review. APP-A1's internal order
is hard even though it is one named stage: implementation observations cannot
retrospectively select designs, values, thresholds, or controls.

### A.5 Interpretation ceiling

The strongest admissible positive statement is:

```text
bounded generator-extractor-redistributor shared-carrier
composition candidate
```

The capacity-relaxed one-source arm has asymmetric force: reproduction makes
participant plurality non-load-bearing, while non-reproduction alone leaves
plurality unresolved. It cannot convert operation composition into a coalition
claim. A positive primary result supports bounded functional complementarity
of operations through the common carrier only because no registered valid
proper subset reproduces the gate signature and every operation has a retained
causal path to `R`. Coalition, functional cooperation, coordination, resource
economy, agency, collective identity, native composition while any load-
bearing producer remains, ecology motif/regime, cross-lane recurrence, and
N31+ selection remain blocked.

APP-A0 semantic review grants no APP-A1 capability audit or APP-A2 runtime
authority. Each later stage opens only after the preceding exact package is
reviewed and retained. Until then, Appendix A has no result.

### A.6 Accepted closeout

APP-A2 completed all 19 frozen fresh-runtime arms with zero retry. Every arm
was operationally valid; `GEP` passed the complete signature; every proper
subset was insufficient; each operation and the common carrier retained its
registered causal path to `R`; and retained-only reconstruction was byte-
identical. Closeout validation passed 53/53.

The accepted result is `supported_bounded_candidate` at exactly the A.5
ceiling. Physical participant plurality and tested operation order are non-
load-bearing in the frozen fixture, and no external producer is load-bearing.
The main P2-I2 result is unchanged. DEC-074/CHG-079 close Appendix A without
opening any blocked claim or subsequent work.

## Appendix B — P2-I2-grounded three-operation shared-pool composition

**Status:** semantic and process authority owner-accepted under
DEC-075/CHG-080; APP-B1 complete; APP-B2/B3 technically complete with owner
result acceptance pending; APP-B4 technically complete with owner result
accepted and closed under DEC-094/CHG-099

### B.1 Exact question

> Can physically distinct generative, extractive, and redistributive operations
> act through one accepted P2-I2 common carrier so that complete GEP produces a
> later native response not reproduced by any valid proper subset—and which of
> current state, active ordered history, or their hybrid carries that relation?

Appendix B tests whether Appendix A's abstract operation composition can be
realized through the already accepted P2-I2 state-carried, history-carried, and
hybrid causal architectures. It is a parallel experiment, not a replication
or synthesis of Appendix A.

### B.2 Outcome quarantine and inherited authority

Appendix B may inherit only the pre-outcome abstract G/E/P meanings,
participant/operation separation, complete-subset structure, one-source and
role-rotation diagnostics, fresh-runtime discipline, and carrier-lineage
requirements. It may not read or use Appendix A runtime evidence,
reconstruction, measured values, successful quantities, thresholds,
realization choices, or conclusions in design, calibration, execution, or
analysis.

The accepted P2-I2 authorities—not Appendix A outcomes—own the three carrier
temporalities, response semantics, restoration identities, controls, runtime
tolerance, and normalized-margin estimator. APP-B1 must bind exact source
identities and decide reuse item by item. Estimator reuse requires unchanged
response unit/window, numeric domain, normalization, floor, and tolerance. The
move from two leave-one comparators to seven proper subsets requires a new
candidate-free pure-analysis extension even if no new matched-null invocation
is needed.

### B.3 Mode-indexed causal projections

All modes use the same physical G/E/P operations, quantities, timing slots,
participant roles, subset rows, response target/window, diversion semantics,
and cost policy wherever admitted machinery permits. Only the causal carrier
read changes:

```text
state_carried:
  G/E/P alter one live common carrier C
  R reads complete current C only
  operation history is audit-only

history_carried:
  native committed G/E/P rows enter one active source-label-free H_C
  R reads H_C through M_H only
  current C is excluded from the response path

hybrid:
  G/E/P alter C and H_C
  R reads [C, M_H] through one common native response path
```

Operations must be grounded in committed physical runtime events. History
tokens derive from frozen operation properties such as channel, direction, or
transfer kind—not participant identity. Admission excludes diversion,
materialization, neutral-contact, response, control-debit, reset/load, and
private-partition traffic.

### B.4 Primary matrix and estimator

Each mode executes the same eight valid rows:

```text
reference, G, E, P, GE, GP, EP, GEP
```

An omitted operation's participant remains active and pays matched cost, but
its operation follows a verified no-influence diversion. Valid zero-effect
rows are scientific zeros. Every proper subset must be operationally valid
before evaluation.

Within one frozen `mode x seed x operation_order` tuple:

```text
strongest_proper_subset_response = max(reference, G, E, P, GE, GP, EP)
primary_margin = accepted normalized response margin of
                 GEP versus strongest_proper_subset_response
```

The maximum may not cross mode, seed, or physical-order boundaries. APP-B1
must freeze the exact positive resolution without inspecting candidate
outcomes.

### B.5 Required causal and discriminator layers

A positive mode disposition requires all three layers:

1. `GEP` exceeds every valid proper subset under the frozen normalized rule.
2. Withdrawing each operation changes `R`, or changes a registered mediator
   whose independent intervention changes `R`.
3. Carrier clamp/freeze, private partitions, and controller substitution show
   that the relation is carried by the registered common path.

Mode-specific requirements are:

```text
state_carried:
  different valid histories + equal complete final C -> equal R

history_carried:
  equal relevant final C + different active H_C -> different R
  history clamp changes R
  state-only C intervention does not

hybrid:
  execute the complete 2 x 2 factorial of reference/candidate C and H_C
  both component interventions affect the same response path
```

No nonlinear synergy is required. The state discriminator distinguishes
current-condition composition from history dependence; the history and hybrid
discriminators identify whether formation history is load-bearing.

### B.6 Participant, operation, and controls

Retain one capacity-relaxed source performing `G -> E -> P` and three cyclic
participant-to-operation assignments. Reproduction by one source makes
participant plurality non-load-bearing; non-reproduction alone leaves it
unresolved. The relation must follow physical operation type rather than fixed
participant identity. Source-label permutation remains mandatory.

Private partitions, direct participant-addressed reads, controller-computed
aggregates, carrier/history clamps, matched diversions, operation withdrawals,
one-source execution, and cyclic role rotations are controls or scope
diagnostics. They do not authorize cooperation, coalition, or participant
complementarity language.

### B.7 Compact authorized cycle

```text
APP-B0:
  this semantic authority + checklist + mode-indexed hypothesis + claim ceiling

APP-B1:
  native-first accepted-source audit
  -> exact physical G/E/P mapping in all three modes
  -> Appendix A outcome-quarantine proof
  -> metric/estimator reuse decision and seven-subset pure extension
  -> realization, controls, conformance, finite matrix, and execution freeze

APP-B2:
  one finite fresh-runtime mode-indexed campaign
  -> retained-only reconstruction and control resolution
  -> one mode-separated Appendix B conclusion
```

DEC-075 waives intermediate owner checkpoints inside this exact cycle.
Candidate-blind implementation corrections and pre-model infrastructure
diagnostics may proceed if recorded under APP-B1/B2 and if they do not change
scientific values, controls, claim semantics, or attempt ceilings. One
pre-execution authority commit is permitted if required for clean-HEAD
one-shot binding. There is one scientific campaign claim and zero scientific
retry. A consumed campaign cannot be rerun without new owner authority.
Final result acceptance and the result commit remain separate owner actions.

### B.8 Interpretation ceiling

The strongest positive result is:

```text
bounded P2-I2-grounded three-operation shared-pool composition candidate
```

Mode dispositions remain separate: `state_carried composition`,
`history_carried composition`, and `hybrid composition`. “Bounded operation-
complementarity through a common carrier” is allowed only where every proper
subset is valid and insufficient and every operation retains a causal path to
`R`.

Cooperation, coordination, coalition, participant complementarity, agency,
resource economy, native history composition while the adapter is
load-bearing, motif/regime, cross-appendix recurrence, cross-lane recurrence,
and N31+ selection remain blocked. Appendix B cannot alter the accepted main
P2-I2 or Appendix A results.
