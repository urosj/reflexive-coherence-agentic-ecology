# P2-I2 APP-B4 activation readiness

## Disposition

The frozen 75-arm APP-B4 panel is technically ready for one governed runtime
campaign after owner review and a containing authority commit. The activation
preparation changes no scientific row, operation, amount, schedule, response,
classifier, claim ceiling, attempt count, or retry ceiling.

No APP-B4 model, producer, arm, response, claim, runtime output, or
reconstruction has occurred in this preparation.

## Blocker found and bounded correction

The runner retained at `863d73c` expected a future activation artifact to
contain `authority_head` equal to the hash of the same commit that first
contained that artifact. That identity is impossible to construct honestly:
adding the hash changes the artifact, which changes the commit hash again.

The correction uses the already accepted two-level authority pattern:

1. `863d73c956286efb631d7a6fdd579837325dc2c0` remains the immutable
   implementation commit and must be an ancestor of the invocation commit.
2. The invocation command supplies the exact activation-containing HEAD.
3. The runner requires that supplied HEAD to equal actual HEAD.
4. Activation, correction, and owner-acceptance bytes must all be tracked and
   byte-identical to that HEAD.
5. The RCAE worktree/index and the pinned graph repository must both be clean.
6. The permanent exclusive claim is created only after these checks pass.

This removes only the self-reference. The original frozen runner hash is
retained in the correction artifact, the corrected runner hash is recorded
separately, and every other frozen implementation hash still matches.

## Prospective single campaign

The activation retains the exact frozen command shape:

```text
.venv/bin/python -B <APP-B4 runner>
  --freeze <inactive 75-arm freeze>
  --activation <live activation>
  --claim <permanent campaign claim>
  --output <single cumulative runtime evidence>
  --expected-head <activation-containing HEAD>
  --graph-root <relative graph repository>
```

All paths in retained artifacts are repository-relative. Parent and all 75
fresh child processes must use lexical `.venv/bin/python -B`. The campaign has
one attempt, zero scientific retry, and zero infrastructure retry. A claim
survives failure. Reconstruction is read-only from the retained aggregate.

## Candidate-free validation

The retained validation passes these boundaries:

- all three authority artifacts are canonical and digest-valid;
- immutable implementation ancestry is satisfiable;
- the original runner matches the inactive freeze;
- the corrected runner matches the correction record;
- all nine other frozen implementation files remain unchanged;
- scientific rows changed: `0`;
- the exact normalized `.venv` command uses `{ACTIVATION_HEAD}` once;
- tracked-byte, clean-HEAD, permanent-claim, and zero-retry guards are present;
- no owner-acceptance artifact exists yet;
- claim, runtime output, and reconstruction are absent;
- no persisted absolute path exists in the candidate package.

A direct inactive-start test also exits before the claim because owner
acceptance is absent. The claim, output, and reconstruction remain absent
after that test.

## Preparation process ledger

```text
read-only source/data inspection processes = 3
syntax/static-source inspection processes = 3
activation builder processes = 1
activation validator processes = 4
  failed before science = 1 (validator-only `0 is False` assertion)
  passed check-only = 1
  passed retained = 2 (first superseded after exact validator-byte binding)
inactive runner-refusal processes = 1
total repository `.venv` processes = 12

PyGRC imports = 0
models / producers / arms / responses = 0/0/0/0
campaign claims / runtime outputs / reconstructions = 0/0/0
scientific rows or semantics changed = 0
```

The syntax compiler's generated bytecode cache was removed immediately; no
cache is retained. The validator-only failure and superseded receipt occurred
before any scientific import or execution and are included rather than hidden.

## Review boundary

The next authorized action, if the owner accepts this exact package, is to add
the owner-acceptance overlay and commit the activation/correction/validation,
corrected runner, precommitted closeout validator, governance records, and
pending interpretative synthesis together. Only the resulting clean committed
HEAD can be supplied to the runner.

That commit would authorize exactly one APP-B4 campaign. It would not itself
assign or accept the scientific result. The runtime, retained-only
reconstruction, and closeout validation must still return uncommitted for
result review.

## Evidence

- [inactive 75-arm freeze](../contracts/p2-i2/app-b4-inactive-75-arm-freeze.json)
- [activation-binding correction](../contracts/p2-i2/app-b4-activation-binding-correction.json)
- [prospective live activation](../contracts/p2-i2/app-b4-live-activation.json)
- [candidate-free activation validation](../contracts/p2-i2/app-b4-activation-candidate-validation.json)
- [corrected one-shot runner](../scripts/p2_i2_app_b4_run.py)
- [activation builder](../scripts/p2_i2_app_b4_activation_freeze.py)
- [activation validator](../scripts/p2_i2_app_b4_activation_validate.py)
- [precommitted closeout validator](../scripts/p2_i2_app_b4_closeout_validate.py)
