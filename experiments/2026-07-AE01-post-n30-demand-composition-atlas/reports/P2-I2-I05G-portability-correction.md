# P2-I2-I05G I03 realization/conformance portability correction

Date: 2026-07-14

Status: `P2-I2-I05G-I03-GROUP-REVIEW-READY`

## Scope and authority

I05G is the third bounded group in the accepted I05D correction order. It is
owned by `P2-I2-DEC-037` and `P2-I2-CHG-030`, with source authority fixed at
commit `99c64dd20db1cf83b79e8bfdf2ac956f7ec46b50`.

The exact scope is the accepted `i03_realization_and_conformance` group:

- 30 affected files;
- 201 accepted I05D findings;
- 20 historical JSON artifacts;
- 9 historical Python sources;
- 1 historical report.

No fourth correction group is authorized by this closeout.

## Correction

The corrected projections replace machine-local graph, interpreter, virtual
environment, base-runtime, temporary-output, and attachment locations with
repository-relative or explicit logical identities. Machine-selecting shebangs
are removed, and the three conformance harnesses derive the sibling PyGRC
checkout from the RCAE repository location while serializing only logical
runtime identities.

Slash-leading I03F JSON-pointer strings are represented as ordered segment
lists. The validator proves that the legacy and structured forms resolve the
same contract targets. Projected command strings are expressly non-executable.

Every changed JSON declares its source commit, source SHA-256, decision,
change, and `scientific_change: false`. The additive lineage manifest binds all
30 historical and projected byte identities. Historical evidence is retained
unchanged at the source commit; the current files are portable projections, not
replacement raw receipts.

## Scientific and runtime boundary

This correction does not alter any accepted I03 realization class, causal
factorization, intervention, private partition, producer boundary, restoration
identity, conformance outcome, family composition, or operational target.

The activity performs no PyGRC import or model instantiation, no conformance or
scientific execution, no candidate or control execution, no calibration/null
builder or wrapper invocation, and no retry. Validation is static and runs only
through `.venv/bin/python`.

Two validator entry-point starts were used. The first failed closed before
writing output because the deterministic projector omitted one admitted owner
attachment identity. Correcting that validator-only mapping required no source
artifact change. The second start produced the retained result. Both starts
remained within the frozen ceiling of three; all other Python-process counts
remain zero.

## Validation disposition

The governed validator verifies the exact I05D group, reconstructs all source
bytes from the accepted commit, checks all 30 lineage pairs, reconstructs every
JSON and text projection exactly, proves pointer-target equivalence, scans the
group and I05G package for persisted absolute paths, checks prior I05F
acceptance identities, and enforces the zero-execution boundary.

The retained validation artifact is the progression evidence for owner review.
I05G does not authorize a commit by itself and does not pass `CAL-GATE`.
